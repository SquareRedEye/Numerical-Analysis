#pragma once
namespace soop {
	void clearMemory(double **, unsigned short); // очистка памяти под двумерный массив
	double roundBy(double &, unsigned short); // округлить число до n знаков после запятой
}